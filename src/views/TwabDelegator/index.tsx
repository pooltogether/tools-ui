import Layout from '@components/Layout'
import { PagePadding } from '@components/Layout/PagePadding'

export const TwabDelegator: React.FC = (props) => {
  return (
    <Layout>
      <PagePadding>
        <h1>TWAB Delegator</h1>
      </PagePadding>
    </Layout>
  )
}
